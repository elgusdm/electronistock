# Configuración para Railway.app
version: 2

services:
  web:
    build: .
    env:
      PORT: $PORT
      DB_HOST: $DB_HOST
      DB_NAME: $DB_NAME
      DB_USER: $DB_USER
      DB_PASSWORD: $DB_PASSWORD
      DB_PORT: $DB_PORT
      SECRET_KEY: $SECRET_KEY